#include <App.h>
#include <UI/UI.h>

use namespace lingshin;
use namespace std::chrono_literals;
use enum Controller::Status;
use enum Controller::Phase;

int main(int argc, char *argv[]) { Tui.loop(); }
